# Core dependencies for Aquabilidad workflows
# These should match or be compatible with CivicStream core

# Workflow framework (imported from CivicStream)
networkx>=3.0

# Data processing
pydantic>=2.0

# Date/time handling
python-dateutil>=2.8

# Testing
pytest>=7.0
pytest-asyncio>=0.21